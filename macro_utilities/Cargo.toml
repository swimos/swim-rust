[package]
name = "macro_utilities"
version = "0.1.0"
authors = ["Swim Inc. developers info@swim.ai"]
edition = "2021"

[dependencies]
proc-macro2 = { workspace = true }
syn = { workspace = true, features = ["printing", "visit", "visit-mut", "parsing"] }
quote = { workspace = true }
