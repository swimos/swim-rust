[package]
name = "swimos_errors"
version = "0.1.0"
authors = ["Swim Inc. developers info@swim.ai"]
edition = "2021"
description = "SwimOS error utilties"
license = "Apache-2.0"

[dependencies]
swimos_algebra = { path = "../swimos_algebra" }

[dev-dependencies]
