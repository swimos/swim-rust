[package]
name = "server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
swim = { path = "../../../swim", features = ["server", "agent"] }
tokio = { version = "1.22", features = ["rt-multi-thread", "macros", "signal"]}
rand = "0.8"
