@config {
    @client {
        buffer_size: 2
        router: {
            retry_strategy: @exponential(max_interval: 30, max_backoff: "indefinite"),
            idle_timeout: 100
            conn_reaper_frequency: 6000
            buffer_size: 15
        }
    }
}