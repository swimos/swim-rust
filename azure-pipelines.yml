trigger: ["master", "server-prototype", "ws-move", "stores"]
pr: ["master", "server-prototype", "ws-move", "stores"]

variables:
  RUSTFLAGS: -Dwarnings

jobs:
  - template: ci/azure-test-stable.yml
    parameters:
      name: test_crates
      rust: 1.55.0
      displayName: Test all crates
      cross: eq(variables['Build.SourceBranch'], 'refs/heads/master')
