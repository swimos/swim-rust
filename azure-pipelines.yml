trigger: ["master", "server-prototype"]
pr: ["master", "server-prototype", "forms_mk2"]

variables:
  RUSTFLAGS: -Dwarnings

jobs:
  - template: ci/azure-test-stable.yml
    parameters:
      name: test_crates
      rust: 1.44.1
      displayName: Test all crates
      cross: eq(variables['Build.SourceBranch'], 'refs/heads/master')

  - template: ci/azure-clippy.yml
    parameters:
      rust: 1.44.1
      name: clippy

  - template: ci/azure-fmt.yml
    parameters:
      rust: 1.44.1
      name: rustfmt

  - template: ci/azure-check-docs.yml
    parameters:
      rust: 1.44.1
      name: docs
