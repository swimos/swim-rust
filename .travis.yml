language: rust

os:
  - linux
  - osx
  - windows

rust:
  - stable
cache: cargo

before_script:
  - rustup component add clippy

script:
  - cargo rustc -- -D warnings
  - cargo test
  - cargo clippy --all-targets --all-features -- -D warnings
