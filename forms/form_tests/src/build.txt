#![feature(prelude_import)]
#![no_std]
#[prelude_import]
use ::std::prelude::v1::*;
#[macro_use]
extern crate std;
use form_derive::Form;
use form_model::Form;

struct Parent {
    a: i32,
    b: i32,
    c: Child,
}

struct Child {
    a: i32,
}


// let _r = parent.try_into_value();
